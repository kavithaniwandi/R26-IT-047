import RequestHistory from "../components/RequestHistory";

export default function History() {
  return (
    <div style={{ minHeight: "100vh", background: "#f0f4f8", paddingTop: "32px" }}>
      <RequestHistory />
    </div>
  );
}
